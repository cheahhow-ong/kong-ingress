apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: upstream-jwt
  annotations:
    kubernetes.io/ingress.class: "kcorp"
config:
  header: "Authorization"
plugin: "upstream-jwt"