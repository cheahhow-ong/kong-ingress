apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: first-time-mobile-password-grant
  annotations:
    konghq.com/plugins: basic-auth, request-transformer-first-time-mobile-password-grant
    konghq.com/strip-path: "true"
    kubernetes.io/ingress.class: "arief-test"
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: httpbin
          servicePort: 80
        path: /v1/first-time/mobile/password/grant
